#include <bits/stdc++.h>

using namespace std;

#define PI 3.141592653

int main(void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);

	long long x, y; cin >> x >> y;
	
	cout << (x % 10) + (y % 10) << "\n";
	return (0);
}