#include <bits/stdc++.h>

using namespace std;

#define PI 3.141592653

int main(void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);

	int x; cin >> x;
	long long r = 1LL * x * (x + 1) / 2;
	cout << r << "\n";
	return (0);
}